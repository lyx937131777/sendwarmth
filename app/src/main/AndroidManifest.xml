<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sendwarmth">

    <application
        android:name=".util.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".NewInterestingActivityActivity"/>
        <activity android:name=".MyInformationActivity" />
        <activity android:name=".PensionInstitutionActivity" />
        <activity android:name=".FriendsCircleActivity" />
        <activity android:name=".HealthBroadcastActivity" />
        <activity android:name=".InterestringActivityActivity" />
        <activity android:name=".OrderingActivity" />
        <activity android:name=".ServiceWorkActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegisterActivity" />
    </application>

</manifest>